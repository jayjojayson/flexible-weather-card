name: Upload Release Asset

# Startet nur, wenn ein neues Release veröffentlicht wird
on:
  release:
    types: [published]

# Schreibrechte sind notwendig für den Upload
permissions:
  contents: write

jobs:
  upload-asset:
    runs-on: ubuntu-latest

    steps:
      # 1. Code auschecken (damit die Datei src/flexible-weather-card.js verfügbar ist)
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Die existierende Datei aus dem src-Ordner an das Release anhängen
      - name: Upload Release Asset
        uses: softprops/action-gh-release@v2
        with:
          files: src/flexible-weather-card.js
    