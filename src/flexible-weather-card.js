/**
 * Flexible Weather Card (Bundled)
 * Generated by build.js
 * Source: dist/flexible-weather-card.js + dist/flexible-weather-card-editor.js
 */

/* * Flexible Weather Card - MAIN FILE
 * Dateiname: flexible-weather-card.js
 * Version: 4.11
 * Features: 
 * - FIX: Deutsche Übersetzungen funktionieren nun auch im "Manuellen Modus"
 * - Alle manuellen Text-Sensoren (Beliebiger Sensor 1-3) werden geprüft und übersetzt
 */

const LitElement = customElements.get("ha-lit-element") || Object.getPrototypeOf(customElements.get("home-assistant-main"));
const html = LitElement.prototype.html;
const css = LitElement.prototype.css;

const loadCardHelpers = async () => {
  if (window.loadCardHelpers) return window.loadCardHelpers();
  if (customElements.get("hui-view")) {
    const element = document.createElement("hui-view");
    if (element.getCardHelpers) return element.getCardHelpers();
  }
  return undefined;
};

class FlexibleWeatherCard extends LitElement {
  static get properties() {
    return {
      hass: {},
      config: {},
      _childCards: { state: true },
    };
  }

  constructor() {
    super();
    this._childCards = [];
  }

  static get styles() {
    return css`
      :host { display: block; }
      
      ha-card {
        overflow: hidden;
        background: var(--ha-card-background, var(--card-background-color, white));
        box-shadow: var(--ha-card-box-shadow, none);
        border-radius: var(--ha-card-border-radius, 12px);
        transition: all 0.3s ease-out;
        padding-bottom: 16px; 
      }
      ha-card.compact { padding-bottom: 0 !important; }

      /* CONTENT WRAPPER */
      .content-wrapper {
        display: flex;
        flex-direction: column;
        gap: 0;
      }
      .content-wrapper.layout-right {
        flex-direction: row;
        align-items: stretch;
      }
      .content-wrapper.layout-right > .weather-section {
        flex: 1; min-width: 0;
        border-right: 1px solid var(--divider-color);
      }
      .content-wrapper.layout-right > .extras-container-wrapper {
        flex: 1; min-width: 0;
        padding-top: 56px; 
      }

      /* SECTIONS */
      .weather-section {
        display: flex;
        flex-direction: column;
        gap: 16px; 
      }

      /* EXTRAS CONTAINER (Child Cards) */
      .extras-section {
        --ha-card-background: transparent;
        --ha-card-box-shadow: none;
        --ha-card-border-width: 0px;
        --ha-card-border-color: transparent;
        --ha-card-border-style: none;
        --ha-card-border-radius: 12px; 
        padding: 16px 16px 0 16px; 
      }
      
      .extras-section.compact {
        padding: 0 !important;
        --ha-card-border-radius: 0px;
      }
      
      /* Spezifische Abstände je nach Position */
      .weather-section .extras-section { padding-bottom: 0; }
      .weather-section .extras-section.compact { padding-bottom: 0; }

      .extra-cards-grid { display: grid; gap: 12px; }

      /* --- COMPONENTS --- */
      .card-header { padding: 16px; font-weight: bold; font-size: 1.2em; display: flex; justify-content: space-between; align-items: center; }
      
      /* PADDING LOGIK */
      .current-weather-container { 
        display: flex; 
        justify-content: center; 
        align-items: center; 
        padding: 8px 16px 8px 16px; 
        gap: 24px; 
        font-weight: 300; 
      }
      .current-weather-container.no-title { 
        padding-top: 28px; 
      }

      .weather-main { font-size: 3.5em; line-height: 1; font-weight: 400; display: flex; align-items: flex-start; transition: color 0.3s ease; }
      .weather-main.temp-very-cold { color: steelblue; }
      .weather-main.temp-cold { color: #2196F3; }
      .weather-main.temp-moderate { color: #4CAF50; }
      .weather-main.temp-warm { color: orange; }
      .weather-main.temp-hot { color: #F44336; }
      .weather-main .unit { font-size: 0.4em; margin-top: 8px; margin-left: 4px; opacity: 0.6; font-weight: 300; color: var(--primary-text-color); }

      .weather-details { display: flex; flex-direction: column; justify-content: center; gap: 6px; text-align: left; font-size: 1.4em; opacity: 0.9; border-left: 1px solid var(--divider-color); padding-left: 16px; }
      .detail-item { display: flex; align-items: baseline; gap: 4px; white-space: nowrap; }
      .detail-val { font-weight: 500; }
      .detail-unit { font-size: 0.6em; opacity: 0.7; }

      .weather-entity-view { padding: 0 24px 8px 24px; display: flex; flex-direction: column; gap: 24px; }
      .weather-entity-view.no-title { padding-top: 28px; }
      
      .we-top-row { display: flex; justify-content: space-between; align-items: center; }
      .we-state { display: flex; align-items: center; gap: 16px; }
      .we-icon { --mdc-icon-size: 64px; color: var(--paper-item-icon-color); }
      .we-state-text { font-size: 1.6em; font-weight: 400; }
      .we-temp-block { text-align: right; }
      .we-temp-big { font-size: 3em; font-weight: 500; line-height: 1; }
      .we-temp-unit { font-size: 0.5em; vertical-align: top; opacity: 0.7; }
      .we-hl-temp { font-size: 0.9em; opacity: 0.6; margin-top: 4px; }
      .we-attributes { display: flex; flex-direction: column; gap: 12px; }
      .we-attr-row { display: flex; align-items: center; justify-content: space-between; font-size: 1.1em; }
      .we-attr-label { display: flex; align-items: center; gap: 12px; opacity: 0.9; }
      .we-attr-icon { color: var(--primary-color); opacity: 0.8; }

      .iframe-grid { display: grid; gap: 12px; margin: 0 16px; }
      .iframe-grid.compact { margin: 0; background: var(--divider-color); }
      .iframe-wrapper { position: relative; overflow: hidden; border-radius: 12px; box-shadow: inset 0 0 0 1px rgba(255,255,255,0.1); background: rgba(0,0,0,0.1); display: flex; align-items: center; justify-content: center; }
      .iframe-grid.compact .iframe-wrapper { border-radius: 0; box-shadow: none; }
      iframe { border: none; width: 100%; height: 100%; display: block; border-radius: inherit; }
      img.iframe-content { width: 100%; height: 100%; object-fit: cover; display: block; }

      .entity-grid { display: grid; gap: 12px; padding: 0 16px; }
      ha-card.compact .entity-grid { padding: 16px; }
      
      ha-card.pos-bottom .entity-grid { padding-top: 8px; }

      .weather-item { display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center; padding: 12px 16px; background: rgba(var(--rgb-primary-text-color), 0.04); border-radius: 12px; transition: all 0.2s ease-in-out; cursor: pointer; user-select: none; border: 1px solid transparent; gap: 4px; }
      .weather-item.horizontal { flex-direction: row; justify-content: space-between; text-align: left; gap: 12px; }
      .weather-item.horizontal .icon { margin: 0; }
      .weather-item.horizontal .state { margin: 0; }
      .weather-item:hover { background: rgba(var(--rgb-primary-text-color), 0.08); }
      .weather-item.active { background: rgba(var(--rgb-primary-color), 0.15); border: 1px solid rgba(var(--rgb-primary-color), 0.5); }
      .weather-item.active .icon { color: var(--primary-color); filter: drop-shadow(0 0 2px rgba(var(--rgb-primary-color), 0.4)); }
      .weather-item.active .state { color: var(--primary-color); }
      .icon { margin: 0; color: var(--paper-item-icon-color); transition: color 0.3s; }
      .state { font-weight: bold; margin: 0; transition: color 0.3s; }
      .label { opacity: 0.7; font-size: 0.8em; }
      .weather-item.horizontal .content-wrapper { display: flex; flex-direction: column; align-items: flex-start; }
    `;
  }

  async updated(changedProps) {
    if (changedProps.has("config")) {
        this._createChildCards();
    }
    if (changedProps.has("hass") && this.hass && this._childCards) {
        this._childCards.forEach(card => {
            if (card) card.hass = this.hass;
        });
    }
  }

  async _createChildCards() {
      if (!this.config || !this.config.cards) {
          this._childCards = [];
          return;
      }
      const helpers = await loadCardHelpers();
      const newCards = [];
      for (const cardConfig of this.config.cards) {
          try {
              const configToUse = cardConfig.card_config || cardConfig; 
              if(configToUse) {
                  const element = await helpers.createCardElement(configToUse);
                  if (this.hass) { element.hass = this.hass; }
                  newCards.push(element);
              }
          } catch (e) {
              const errEl = document.createElement("div");
              errEl.innerHTML = `Fehler: ${e.message}`;
              errEl.style.color = "red";
              newCards.push(errEl);
          }
      }
      this._childCards = newCards;
  }

  _handleAction(item) {
    if (!this.hass || !item.entity) return;
    const entityId = item.entity;
    const domain = entityId.split(".")[0];
    const toggleDomains = ["light", "switch", "input_boolean", "fan", "automation"];
    if (toggleDomains.includes(domain)) {
      this.hass.callService("homeassistant", "toggle", { entity_id: entityId });
    } else {
      const event = new CustomEvent("hass-more-info", { detail: { entityId }, bubbles: true, composed: true });
      this.dispatchEvent(event);
    }
  }

  _isImage(url) {
    if (!url) return false;
    return url.match(/\.(jpeg|jpg|gif|png|webp)(?:$|\?|;)/i) != null;
  }

  _formatVal(val) {
    if (val === undefined || val === null || isNaN(val)) return "-";
    return parseFloat(val).toLocaleString(undefined, { minimumFractionDigits: 0, maximumFractionDigits: 1 });
  }

  _getSensorData(entity) {
    if (!entity) return { val: null, unit: "" };
    const s = this.hass.states[entity];
    if (!s) return { val: null, unit: "" };
    return { val: s.state, unit: s.attributes.unit_of_measurement || "" };
  }

  _getWeatherIcon(state) {
    const map = {
      'clear-night': 'mdi:weather-night', 'cloudy': 'mdi:weather-cloudy', 'fog': 'mdi:weather-fog', 'hail': 'mdi:weather-hail',
      'lightning': 'mdi:weather-lightning', 'lightning-rainy': 'mdi:weather-lightning-rainy', 'partlycloudy': 'mdi:weather-partly-cloudy',
      'partly-cloudy': 'mdi:weather-partly-cloudy',
      'pouring': 'mdi:weather-pouring', 'rainy': 'mdi:weather-rainy', 'snowy': 'mdi:weather-snowy', 'snowy-rainy': 'mdi:weather-snowy-rainy',
      'sunny': 'mdi:weather-sunny', 'windy': 'mdi:weather-windy', 'windy-variant': 'mdi:weather-windy-variant', 'exceptional': 'mdi:alert-circle-outline'
    };
    return map[state] || 'mdi:weather-cloudy';
  }

  _translateCondition(state) {
     const germanMapping = {
        'clear-night': 'Klare Nacht',
        'cloudy': 'Bewölkt',
        'fog': 'Nebel',
        'hail': 'Hagel',
        'lightning': 'Gewitter',
        'lightning-rainy': 'Gewitter mit Regen',
        'partlycloudy': 'Teils bewölkt',
        'partly-cloudy': 'Teils bewölkt', 
        'pouring': 'Starkregen',
        'rainy': 'Regen',
        'snowy': 'Schnee',
        'snowy-rainy': 'Schneeregen',
        'sunny': 'Sonnig',
        'windy': 'Windig',
        'windy-variant': 'Windig (Variabel)',
        'exceptional': 'Warnung'
     };
     
     // 1. Zuerst die manuelle deutsche Liste prüfen (ÜBERSCHREIBT HA)
     if (germanMapping[state]) {
         return germanMapping[state];
     }

     // 2. Fallback auf HA System, falls der Status unbekannt ist
     if (this.hass && this.hass.localize) {
         const localized = this.hass.localize(`component.weather.state._.${state}`) || this.hass.localize(`state.weather.${state}`);
         if (localized) return localized;
     }
     
     // 3. Wenn alles fehlschlägt: Originalzustand anzeigen
     return state;
  }

  render() {
    if (!this.config || !this.hass) return html``;

    const fontSize = this.config.font_size || "14px";
    const standardHeight = this.config.iframe_height || "200px";
    const heroHeight = this.config.hero_iframe_height || standardHeight;
    const iframeCols = this.config.iframe_cols || 1;
    const entityCols = this.config.entity_cols;
    const layoutMode = this.config.layout_mode || "default";
    const disablePadding = this.config.disable_padding || false;
    const compactGap = this.config.compact_gap || "2px";
    const entitiesPos = this.config.entities_position || "top";
    const weatherSource = this.config.weather_source || "none";
    
    const cardsCols = this.config.cards_cols || 1;
    const cardsPos = this.config.cards_position || "bottom"; 
    const cardsCompact = this.config.cards_compact || false; 
    const cardsGap = this.config.cards_gap || "2px"; 

    let iframeGridStyle = `grid-template-columns: repeat(${iframeCols}, 1fr);`;
    if (disablePadding) iframeGridStyle += ` gap: ${compactGap};`;

    const entityGridStyle = entityCols 
      ? `grid-template-columns: repeat(${entityCols}, 1fr);` 
      : `grid-template-columns: repeat(auto-fit, minmax(100px, 1fr));`;
      
    let extraCardsGridStyle = `grid-template-columns: repeat(${cardsCols}, 1fr);`;
    if (cardsCompact) { extraCardsGridStyle += ` gap: ${cardsGap};`; } 
    else { extraCardsGridStyle += ` gap: 12px;`; }

    const iframes = this.config.iframes || [];
    const entities = this.config.entities || [];
    const hasTitle = !!this.config.title;

    /* --- DATEN MANUELL --- */
    let wTemp = null, uTemp = "°C";
    let wHum = null, uHum = "%";
    let wPress = null, uPress = ""; 
    let wExt1 = null, uExt1 = "";   
    let wExt2 = null, uExt2 = "";
    let tempClass = "";

    if (weatherSource === 'manual') {
        const tData = this._getSensorData(this.config.temp_entity);
        wTemp = tData.val; uTemp = tData.unit;
        
        const hData = this._getSensorData(this.config.hum_entity);
        wHum = hData.val; uHum = hData.unit;
        
        const pData = this._getSensorData(this.config.press_entity);
        // Auch "Beliebiger Sensor 1" wird nun übersetzt (falls Text):
        wPress = this._translateCondition(pData.val); 
        uPress = pData.unit;
        
        const e1Data = this._getSensorData(this.config.extra_sensor_1);
        // "Beliebiger Sensor 2" übersetzen:
        wExt1 = this._translateCondition(e1Data.val); 
        uExt1 = e1Data.unit;
        
        const e2Data = this._getSensorData(this.config.extra_sensor_2);
        // "Beliebiger Sensor 3" übersetzen:
        wExt2 = this._translateCondition(e2Data.val); 
        uExt2 = e2Data.unit;

        if (this.config.enable_temp_color && wTemp !== null) {
            const t = parseFloat(wTemp);
            if (!isNaN(t)) {
                if (t < 0) tempClass = "temp-very-cold"; 
                else if (t < 10) tempClass = "temp-cold"; 
                else if (t < 22) tempClass = "temp-moderate"; 
                else if (t < 28) tempClass = "temp-warm"; 
                else tempClass = "temp-hot";
            }
        }
    }

    // --- TEMPLATE: WETTER ENTITÄT ---
    let weatherEntityTemplate = html``;
    if (weatherSource === 'entity' && this.config.weather_entity) {
        const wObj = this.hass.states[this.config.weather_entity];
        if (wObj && wObj.attributes) {
            const attr = wObj.attributes;
            const state = wObj.state;
            const temp = attr.temperature;
            const tempUnit = this.hass.config.unit_system.temperature || "°C";
            const pressUnit = this.hass.config.unit_system.pressure || "hPa";
            
            weatherEntityTemplate = html`
                <div class="weather-entity-view ${!hasTitle ? 'no-title' : ''}">
                    <div class="we-top-row">
                        <div class="we-state">
                            <ha-icon class="we-icon" .icon="${this._getWeatherIcon(state)}"></ha-icon>
                            <span class="we-state-text">${this._translateCondition(state)}</span>
                        </div>
                        <div class="we-temp-block">
                            <div class="we-temp-big">${this._formatVal(temp)}<span class="we-temp-unit">${tempUnit}</span></div>
                        </div>
                    </div>
                    <div class="we-attributes">
                        ${attr.pressure ? html`<div class="we-attr-row"><div class="we-attr-label"><ha-icon class="we-attr-icon" icon="mdi:gauge"></ha-icon> Luftdruck</div><div>${this._formatVal(attr.pressure)} ${pressUnit}</div></div>` : ""}
                        ${attr.humidity ? html`<div class="we-attr-row"><div class="we-attr-label"><ha-icon class="we-attr-icon" icon="mdi:water-percent"></ha-icon> Luftfeuchtigkeit</div><div>${attr.humidity} %</div></div>` : ""}
                        ${attr.wind_speed ? html`<div class="we-attr-row"><div class="we-attr-label"><ha-icon class="we-attr-icon" icon="mdi:weather-windy"></ha-icon> Windgeschwindigkeit</div><div>${attr.wind_speed} km/h</div></div>` : ""}
                        ${attr.visibility !== undefined && attr.visibility !== null ? html`<div class="we-attr-row"><div class="we-attr-label"><ha-icon class="we-attr-icon" icon="mdi:eye"></ha-icon> Sichtweite</div><div>${attr.visibility} km</div></div>` : ""}
                        ${attr.uv_index !== undefined ? html`<div class="we-attr-row"><div class="we-attr-label"><ha-icon class="we-attr-icon" icon="mdi:sunglasses"></ha-icon> UV Index</div><div>${attr.uv_index}</div></div>` : ""}
                    </div>
                </div>
            `;
        }
    }

    const iframesTemplate = iframes.length > 0 ? html`
          <div class="iframe-grid ${disablePadding ? 'compact' : ''}" style="${iframeGridStyle}">
            ${iframes.map((frame, index) => {
                const shouldSpan = (iframeCols === 2 && iframes.length % 2 !== 0 && index === 0);
                const style = shouldSpan ? `grid-column: span ${iframeCols};` : "";
                const currentHeight = shouldSpan ? heroHeight : standardHeight;
                const isImg = this._isImage(frame.url);
                return html`<div class="iframe-wrapper" style="height: ${currentHeight}; ${style}">${isImg ? html`<img class="iframe-content" src="${frame.url}">` : html`<iframe src="${frame.url}" scrolling="no"></iframe>`}</div>`;
            })}
          </div>
    ` : html``;

    const entitiesTemplate = entities.length > 0 ? html`
            <div class="entity-grid" style="${entityGridStyle} font-size: ${fontSize};">
            ${entities.map((item) => {
                const stateObj = this.hass.states[item.entity];
                if (!stateObj) return html`<div class="weather-item"><ha-icon icon="mdi:alert-circle" style="color: red;"></ha-icon><div class="label">Fehlt: ${item.entity}</div></div>`;
                const displayState = stateObj.state;
                const unit = stateObj.attributes.unit_of_measurement || "";
                const icon = item.icon || stateObj.attributes.icon || "mdi:help-circle"; 
                const name = item.name || stateObj.attributes.friendly_name || item.entity;
                const domain = item.entity.split(".")[0];
                const isActive = (domain === "light" || domain === "switch" || domain === "input_boolean" || domain === "fan") && stateObj.state === "on";
                const stateText = domain === 'light' || domain === 'switch' ? (stateObj.state === 'on' ? 'An' : 'Aus') : displayState + " " + unit;
                const iconTemplate = html`<ha-icon class="icon" .icon="${icon}"></ha-icon>`;
                const contentTemplate = html`<div class="content-wrapper"><div class="state">${stateText}</div><div class="label">${name}</div></div>`;
                const stateVertical = html`<div class="state">${stateText}</div>`;
                const labelVertical = html`<div class="label">${name}</div>`;
                const itemClasses = `weather-item ${isActive ? 'active' : ''} ${layoutMode === 'horizontal' ? 'horizontal' : ''}`;

                return html`
                <div class="${itemClasses}" @click="${() => this._handleAction(item)}">
                    ${layoutMode === 'horizontal' ? html`${contentTemplate}${iconTemplate}` : layoutMode === 'swap' ? html`${stateVertical}${iconTemplate}${labelVertical}` : html`${iconTemplate}${stateVertical}${labelVertical}`}
                </div>`;
            })}
            </div>
    ` : html``;

    // --- TEMPLATE: CHILD CARDS ---
    const extraCardsTemplate = (this._childCards && this._childCards.length > 0) ? html`
        <div class="extras-section ${cardsCompact ? 'compact' : ''}">
            <div class="extra-cards-grid" style="${extraCardsGridStyle}">
                ${this._childCards.map((card, index) => {
                    const shouldSpan = (cardsCols === 2 && this._childCards.length % 2 !== 0 && index === 0);
                    const style = shouldSpan ? "grid-column: span 2;" : "";
                    return html`<div style="${style}">${card}</div>`;
                })}
            </div>
        </div>
    ` : html``;

    // --- TEMPLATE: WEATHER CONTENT ---
    const weatherContent = html`
        <div class="weather-section">
            ${hasTitle ? html`<div class="card-header">${this.config.title}</div>` : html``}
            
            ${weatherSource === 'entity' ? weatherEntityTemplate : ''}
            ${weatherSource === 'manual' && wTemp !== null ? html`
                <div class="current-weather-container ${!hasTitle ? 'no-title' : ''}">
                    <div class="weather-main ${tempClass}">${this._formatVal(wTemp)}<span class="unit">${uTemp}</span></div>
                    <div class="weather-details">
                        ${wHum !== null ? html`<div class="detail-item"><span class="detail-val">${Math.round(wHum)}</span><span class="detail-unit">${uHum}</span></div>` : ""}
                        ${wPress !== null ? html`<div class="detail-item"><span class="detail-val">${wPress}</span><span class="detail-unit">${uPress}</span></div>` : ""}
                    </div>
                    ${(wExt1 !== null || wExt2 !== null) ? html`
                        <div class="weather-details">
                            ${wExt1 !== null ? html`<div class="detail-item"><span class="detail-val">${wExt1}</span><span class="detail-unit">${uExt1}</span></div>` : ""}
                            ${wExt2 !== null ? html`<div class="detail-item"><span class="detail-val">${wExt2}</span><span class="detail-unit">${uExt2}</span></div>` : ""}
                        </div>
                    ` : ""}
                </div>
            ` : ""}
            
            ${cardsPos === 'middle' ? extraCardsTemplate : ''}

            ${entitiesPos === 'top' ? entitiesTemplate : ''}
            
            ${iframesTemplate}
            
            ${entitiesPos === 'bottom' ? entitiesTemplate : ''}
        </div>
    `;

    return html`
      <ha-card class="${disablePadding ? 'compact' : ''}">
        <div class="content-wrapper ${cardsPos === 'right' ? 'layout-right' : ''}">
            ${weatherContent}
            ${(cardsPos !== 'middle') ? html`<div class="extras-container-wrapper">${extraCardsTemplate}</div>` : ""}
        </div>
      </ha-card>
    `;
  }

  setConfig(config) {
    this.config = config;
  }

  static async getConfigElement() {
    await import("/local/community/flexible-weather-card/flexible-weather-card-editor.js");
    return document.createElement("flexible-weather-card-editor");
  }

  static getStubConfig() {
    return {
      title: "Wetter Übersicht",
      font_size: "16px",
      iframe_height: "200px",
      hero_iframe_height: "300px",
      disable_padding: false,
      compact_gap: "2px",
      iframe_cols: 1,
      entity_cols: 3,
      layout_mode: "default",
      weather_source: "none",
      entities_position: "top",
      enable_temp_color: true,
      cards_cols: 1,
      cards_position: "bottom",
      cards_compact: false,
      cards_gap: "2px",
      cards: [],
      iframes: [],
      entities: []
    };
  }
}

customElements.define("flexible-weather-card", FlexibleWeatherCard);

window.customCards = window.customCards || [];
window.customCards.push({
  type: "flexible-weather-card",
  name: "Flexible Weather Card",
  preview: true,
  description: "Wetterkarte mit Grid Layout und Child-Cards",
});


/* * Flexible Weather Card - EDITOR FILE
 * Dateiname: flexible-weather-card-editor.js
 * Version: 4.3
 * Features: 
 * - UI Cleanup: Überschrift "Zusätzliche Cards Liste" entfernt
 */

class FlexibleWeatherCardEditor extends LitElement {
  static get properties() {
    return {
      hass: {},
      _config: {},
    };
  }

  static get styles() {
    return css`
      .card-config { display: flex; flex-direction: column; gap: 16px; padding-bottom: 24px; }
      .section-header { 
        font-weight: bold; font-size: 1.1em; margin-top: 24px; margin-bottom: 8px; 
        display: block; border-bottom: 1px solid var(--divider-color); padding-bottom: 4px;
        color: var(--primary-text-color);
      }
      .sub-settings { margin-bottom: 16px; }
      .list-row {
        background: var(--secondary-background-color); padding: 12px; border-radius: 8px;
        border: 1px solid var(--divider-color); margin-bottom: 12px;
        display: flex; flex-direction: column; gap: 12px; position: relative;
      }
      .remove-icon-absolute { position: absolute; top: 4px; right: 4px; color: var(--error-color); }
      .entity-options-row { display: flex; gap: 8px; width: 100%; }
      .entity-options-row > * { flex: 1; }
      .iframe-row { display: flex; align-items: center; gap: 8px; margin-bottom: 8px; }
      .iframe-row ha-textfield { flex-grow: 1; }
      .add-button { margin-top: 8px; width: 100%; }
    `;
  }

  setConfig(config) {
    this._config = config;
  }

  _fireConfigChanged(newConfig) {
    const event = new CustomEvent("config-changed", {
      detail: { config: newConfig },
      bubbles: true,
      composed: true,
    });
    this.dispatchEvent(event);
  }

  _genericConfigChanged(ev) {
    this._fireConfigChanged(ev.detail.value);
  }

  /* --- IFRAME LOGIK --- */
  _addIframe() {
    const newIframes = [...(this._config.iframes || [])];
    newIframes.push({ url: "" });
    this._fireConfigChanged({ ...this._config, iframes: newIframes });
  }
  _removeIframe(index) {
    const newIframes = [...(this._config.iframes || [])];
    newIframes.splice(index, 1);
    this._fireConfigChanged({ ...this._config, iframes: newIframes });
  }
  _editIframeUrl(index, value) {
    const newIframes = [...(this._config.iframes || [])];
    newIframes[index] = { ...newIframes[index], url: value };
    this._fireConfigChanged({ ...this._config, iframes: newIframes });
  }

  /* --- ENTITY LOGIK --- */
  _addEntity() {
    const newEntities = [...(this._config.entities || [])];
    newEntities.push({ entity: "", name: "", icon: "" });
    this._fireConfigChanged({ ...this._config, entities: newEntities });
  }
  _removeEntity(index) {
    const newEntities = [...(this._config.entities || [])];
    newEntities.splice(index, 1);
    this._fireConfigChanged({ ...this._config, entities: newEntities });
  }
  _editEntity(index, key, value) {
    const newEntities = [...(this._config.entities || [])];
    newEntities[index] = { ...newEntities[index], [key]: value };
    this._fireConfigChanged({ ...this._config, entities: newEntities });
  }

  /* --- CARD LOGIK --- */
  _addCard() {
    const newCards = [...(this._config.cards || [])];
    newCards.push({ card_config: {} }); 
    this._fireConfigChanged({ ...this._config, cards: newCards });
  }
  _removeCard(index) {
    const newCards = [...(this._config.cards || [])];
    newCards.splice(index, 1);
    this._fireConfigChanged({ ...this._config, cards: newCards });
  }
  _editCardConfig(index, value) {
    const newCards = [...(this._config.cards || [])];
    newCards[index] = { ...newCards[index], card_config: value };
    this._fireConfigChanged({ ...this._config, cards: newCards });
  }


  render() {
    if (!this.hass || !this._config) return html``;

    // --- SCHEMA 1: Allgemein & Wetter ---
    const schemaGeneral = [
      { name: "title", label: "Titel der Karte", selector: { text: {} } },
      
      {
        name: "weather_source",
        label: "Wetterdaten Quelle (Über iFrame)",
        selector: { select: { options: [
            { value: "none", label: "Keine Anzeige" },
            { value: "entity", label: "Wetter Entität (weather.*)" },
            { value: "manual", label: "Manuelle Sensoren" }
        ], mode: "dropdown" } }
      }
    ];

    if (this._config.weather_source === "entity") {
        schemaGeneral.push({ name: "weather_entity", label: "Wetter Integration wählen", selector: { entity: { domain: "weather" } } });
    } else if (this._config.weather_source === "manual") {
        schemaGeneral.push(
            { name: "temp_entity", label: "Temperatur Sensor", selector: { entity: {} } },
            { name: "hum_entity", label: "Luftfeuchte Sensor", selector: { entity: {} } },
            { name: "press_entity", label: "Beliebiger Sensor 1", selector: { entity: {} } },
            { name: "extra_sensor_1", label: "Beliebiger Sensor 2", selector: { entity: {} } },
            { name: "extra_sensor_2", label: "Beliebiger Sensor 3", selector: { entity: {} } },
            { name: "enable_temp_color", label: "Temperatur farblich hervorheben", selector: { boolean: {} } }
        );
    }

    // --- SCHEMA 2: iFrame Einstellungen ---
    const schemaIframeSettings = [
      { name: "disable_padding", label: "Kompaktansicht (Kein Rand bei Bildern)", selector: { boolean: {} } }
    ];
    if (this._config.disable_padding) {
        schemaIframeSettings.push({ name: "compact_gap", label: "Abstand im Kompaktmodus (z.B. 2px)", selector: { text: {} } });
    }
    schemaIframeSettings.push(
      { name: "iframe_height", label: "Höhe Standard (kleine Bilder)", selector: { text: {} } },
      { name: "hero_iframe_height", label: "Höhe Großes Bild (Spalten-Übergreifend)", selector: { text: {} } },
      { name: "iframe_cols", label: "Spalten Bilder / Frames", selector: { number: { min: 1, max: 4, mode: "box" } } }
    );


    // --- SCHEMA 3: Entity Einstellungen ---
    const schemaEntitySettings = [
      { name: "font_size", label: "Schriftgröße (z.B. 16px)", selector: { text: {} } },
      { name: "entity_cols", label: "Spalten Entitäten", selector: { number: { min: 1, max: 8, mode: "box" } } },
      {
        name: "layout_mode",
        label: "Layout Modus Entitäten",
        selector: { select: { options: [
            { value: "default", label: "Standard (Icon oben)" },
            { value: "swap", label: "Wert oben / Icon unten" },
            { value: "horizontal", label: "Nebeneinander (Schmal)" }
        ], mode: "dropdown" } }
      },
      {
        name: "entities_position",
        label: "Position der Entitäten",
        selector: { select: { options: [
            { value: "top", label: "Über den iFrames (Standard)" },
            { value: "bottom", label: "Unter den iFrames" }
        ], mode: "dropdown" } }
      }
    ];

    // --- SCHEMA 4: EXTRA CARDS SETTINGS ---
    const schemaCardsSettings = [
      { name: "cards_cols", label: "Spalten für Extra Cards", selector: { number: { min: 1, max: 4, mode: "box" } } },
      {
        name: "cards_position",
        label: "Position der Extra Cards",
        selector: { select: { options: [
            { value: "bottom", label: "Unten (Unter allem)" },
            { value: "middle", label: "Direkt unter Wetterdaten (Mitte)" },
            { value: "right", label: "Rechts (Nebeneinander)" }
        ], mode: "dropdown" } }
      },
      { name: "cards_compact", label: "Kompaktansicht Cards (Kein Rand)", selector: { boolean: {} } }
    ];
    if (this._config.cards_compact) {
        schemaCardsSettings.push({ name: "cards_gap", label: "Abstand Cards (z.B. 2px)", selector: { text: {} } });
    }

    const iframes = this._config.iframes || [];
    const entities = this._config.entities || [];
    const extraCards = this._config.cards || [];
    const entitySelector = { entity: {} };
    const cardSelector = { object: {} };

    return html`
      <div class="card-config">
        
        <ha-form .hass=${this.hass} .data=${this._config} .schema=${schemaGeneral} .computeLabel=${(s) => s.label} @value-changed=${this._genericConfigChanged}></ha-form>

        <div>
            <span class="section-header">iFrame Settings</span>
            <div class="sub-settings">
                <ha-form .hass=${this.hass} .data=${this._config} .schema=${schemaIframeSettings} .computeLabel=${(s) => s.label} @value-changed=${this._genericConfigChanged}></ha-form>
            </div>
            ${iframes.map((frame, index) => html`
              <div class="iframe-row">
                  <ha-textfield label="URL (Webseite oder Bild-Link)" .value="${frame.url || ''}" @input="${(e) => this._editIframeUrl(index, e.target.value)}"></ha-textfield>
                <ha-icon-button .path=${"M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z"} @click="${() => this._removeIframe(index)}"></ha-icon-button>
              </div>
            `)}
            <mwc-button class="add-button" outlined @click="${this._addIframe}">
              <ha-icon icon="mdi:plus" style="margin-right: 8px;"></ha-icon> iFrame / Bild / GIF hinzufügen
            </mwc-button>
        </div>

        <div>
            <span class="section-header">Entitäten Settings (Wetter, Schalter)</span>
            <div class="sub-settings">
                <ha-form .hass=${this.hass} .data=${this._config} .schema=${schemaEntitySettings} .computeLabel=${(s) => s.label} @value-changed=${this._genericConfigChanged}></ha-form>
            </div>
            ${entities.map((item, index) => html`
              <div class="list-row">
                <ha-icon-button class="remove-icon-absolute" .path=${"M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z"} @click="${() => this._removeEntity(index)}"></ha-icon-button>
                <ha-selector .hass=${this.hass} .selector=${entitySelector} .value=${item.entity} .label=${"Entität auswählen"} @value-changed="${(e) => this._editEntity(index, 'entity', e.detail.value)}"></ha-selector>
                <div class="entity-options-row">
                    <ha-textfield label="Name (optional)" .value=${item.name || ""} @input="${(e) => this._editEntity(index, 'name', e.target.value)}"></ha-textfield>
                    <ha-icon-picker .hass=${this.hass} .value=${item.icon || ""} label="Icon" @value-changed="${(e) => this._editEntity(index, 'icon', e.target.value)}"></ha-icon-picker>
                </div>
              </div>
            `)}
            <mwc-button class="add-button" outlined @click="${this._addEntity}">
              <ha-icon icon="mdi:plus" style="margin-right: 8px;"></ha-icon> Entität hinzufügen
            </mwc-button>
        </div>

        <div>
            <span class="section-header">Zusätzliche Cards Settings</span>
            <div class="sub-settings">
                <ha-form .hass=${this.hass} .data=${this._config} .schema=${schemaCardsSettings} .computeLabel=${(s) => s.label} @value-changed=${this._genericConfigChanged}></ha-form>
            </div>
            
            ${extraCards.map((cardItem, index) => html`
              <div class="list-row">
                <ha-icon-button class="remove-icon-absolute" .path=${"M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z"} @click="${() => this._removeCard(index)}"></ha-icon-button>
                <ha-selector .hass=${this.hass} .selector=${cardSelector} .value=${cardItem.card_config} .label=${"Card Konfiguration (YAML/Objekt)"} @value-changed="${(e) => this._editCardConfig(index, e.detail.value)}"></ha-selector>
              </div>
            `)}
            <mwc-button class="add-button" outlined @click="${this._addCard}">
              <ha-icon icon="mdi:plus" style="margin-right: 8px;"></ha-icon> Card hinzufügen
            </mwc-button>
        </div>

      </div>
    `;
  }
}

customElements.define("flexible-weather-card-editor", FlexibleWeatherCardEditor);

